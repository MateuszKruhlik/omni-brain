# RAG INGESTION LOG & NOTES

## Configuration
- **Model**: `voyage-4-large` (Voyage AI) — configurable via `EMBEDDING_PROVIDER` in `.env`
- **Chunking**: Page-level (1 page = 1 chunk).
- **Metadata**: `title`, `file`, `page`, `tags`.

## Ingest History
- **[YYYY-MM-DD]**: Initial setup.

## TODO
- [ ] Define ingest script/tool (currently manual or OpenAI script).
- [ ] Ingest first batch of files.
